// enum version of ALERT_MODE type
export enum AlertModes {
  ALL = 'all',
  NONE = 'none',
  ON_SUCCESS = 'on-success',
  ON_ERROR = 'on-error'
}

// enum version of AlertStatuses type
export enum AlertStatuses {
  SUCCESS = 'success',
  ERROR = 'error'
}
